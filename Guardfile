# run_at_start: true                 compile files when guard starts
# notifications: true                send notifictions to Growl/libnotify/Notifu

guard :haml, run_at_start: true, default_ext: 'tag' do
  watch(/^.+(\.haml)$/)
end
